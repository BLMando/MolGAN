fitness non ci interessa
ci interessa generalization intesa come process mining

 dare in input delle proprietà --> 1 solo nodo iniziale e 1 solo finale
 generalizzazione della struttura

 raggruppare grafi per numerosità di nodi --> evitiamo di aggiungere PAD --> valutiamo se le prestazioni migliorano
 indicatore che ci dice quanto i grafi sono diversi l'uno dall'altro


appena irrigidiamo i constraint sfarfalla
viste dimensioni diverse --> il 6 capita più volte
se mettiamo vincoli stringenti si sbarella tutto
due attività posso capitare 2 volte di fila es: 8 -> 8


Aggiungere predizione feature temporanee --> quindi generare timestamp
poca variabilità nei nodi ok --> proviamo ad aggiungere variabilità nelle feature generate

ricercare in letteratura a riguardo per le feature

fare tabella di resoconto per ogni grafo
pnml per capire causal relation;
xes le tracce
.igs per i grafi

usare feature nomr_time trace_time prev_event_time
prende paper per valutare le metriche in base al modello per gli instance_graph --> per valutare la bontà del grafo che viene fuori
utilizzare tutte le proprietà degli instance_graph
1) sistemare il modello
2) mostrare grafici --> mandare su teams
3) bontà degli instance_graph --> articolo BIG --> più importante sulla generalization
4) introdurre le feature temporanee 